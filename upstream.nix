# The pi release this flake's module and README were last reconciled against.
# Renovate bumps it; .github/workflows/upstream-sync.yml then diffs the two
# releases and opens a PR bringing the module in line.
{
  npmPackage = "@earendil-works/pi-coding-agent";
  # renovate: datasource=npm depName=@earendil-works/pi-coding-agent
  version = "0.87.1";
}
